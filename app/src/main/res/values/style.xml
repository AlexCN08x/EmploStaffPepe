<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Custom Spinner Style -->
    <style name="CustomSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

    <!-- Custom Dropdown Item Style -->
    <style name="CustomSpinnerItemText" parent="TextAppearance.AppCompat.Widget.TextView.SpinnerItem">
        <item name="android:textColor">#FF0000</item> <!-- Change to desired color -->
        <item name="android:textSize">18sp</item> <!-- Change to desired size -->
    </style>

    <!-- Custom Dropdown Style -->
    <style name="CustomSpinnerDropdownItem" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:textColor">#00FF00</item> <!-- Change to desired color -->
        <item name="android:textSize">16sp</item> <!-- Change to desired size -->
    </style>
</resources>
